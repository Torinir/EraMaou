;eraIM@Sから導入しました

@SHOW_JUEL
PRINTL ..............................................................................
FOR COUNT, 0, 13
	IF COUNT == 3
		LOCAL = 14
	ELSEIF COUNT == 11
		LOCAL = 100
	ELSEIF COUNT == 12
		LOCAL = 15
	ELSE
		LOCAL = COUNT
	ENDIF

	PRINTFORM  %PALAMNAME:LOCAL% Points:{JUEL:LOCAL, 6, RIGHT}

	;改行
	IF (COUNT+1)%4 == 0
		PRINTL 
	ENDIF
NEXT
PRINTL
PRINTL ..............................................................................

@SHOW_ABLUP_SELECT
U = 0
REPEAT 40
	SIF COUNT >= 4 && COUNT <=9
		CONTINUE
	SIF COUNT >= 18 && COUNT <= 19
		CONTINUE
	SIF COUNT >= 24 && COUNT <= 29
		CONTINUE
	SIF COUNT >= 34 && COUNT <= 36
		CONTINUE
	SIF COUNT == 38
		CONTINUE

	;男だとV感覚とレズっ気とレズ中毒は上げられない
	SIF TALENT:122 && (COUNT == 2 || COUNT == 22 || COUNT == 33)
		CONTINUE
	;女だとＢＬっ気とＢＬ中毒は上げられない
	SIF TALENT:122 == 0 && (COUNT == 23 || COUNT == 34)
		CONTINUE
	X = COUNT
	PRINTPLAIN  
	IF X == 0 && TALENT:101 & 2
		SETCOLOR 128, 128, 128
		PRINT [―]
	ELSEIF X == 1 && TALENT:107 & 2
		SETCOLOR 128, 128, 128
		PRINT [―]
	ELSEIF X == 2 && TALENT:103 & 2
		SETCOLOR 128, 128, 128
		PRINT [―]
	ELSEIF X == 3 && TALENT:105 & 2
		SETCOLOR 128, 128, 128
		PRINT [―]
	ELSE
		PRINTFORM [{X, 2}]
	ENDIF
	PRINTFORM %@" %ABLNAME:X%", 18, LEFT%
	;文字揃えその2
	IF X <= 3 || X == 17
	;	PRINT  
	ELSEIF (X >= 10 && X <= 12) || X == 15
	;	PRINT  
	ENDIF	
	PRINTFORM %@"- Lv: {ABL:X}", 3, LEFT%
	CALL DECIDE_ABLUP
	RESETCOLOR
	U += 1
	IF U % 4 == 0
		PRINTL  
	ENDIF
REND 
SIF U % 4 != 0
	PRINTL  

PRINTFORM %@" [99] %MARKNAME:3%", 22, LEFT%
PRINTFORM %@" - Lv: {MARK:3}", 3, LEFT%
CALL DECIDE_ABLUP99
SIF RESULT == 1
	PRINT  *
SIF CSTR:7 != ""
	PRINTFORM   [ 4] %CSTR:7% Sense
CALL DECIDE_ABLUP4
SIF RESULT == 1
	PRINT  *
SIF CSTR:7 != ""
	PRINTFORM   [40] %CSTR:7% Addiction
CALL DECIDE_ABLUP40
SIF RESULT == 1
	PRINT  *

PRINTL 
PRINTL ..............................................................................
PRINTL [999] - Done Raising Abilities

@DECIDE_ABLUP
;Ｃ感覚
IF X == 0
	CALL DECIDE_ABLUP0
;Ｂ感覚
ELSEIF  X == 1
	CALL DECIDE_ABLUP1
;V感覚
ELSEIF X == 2
	CALL DECIDE_ABLUP2
;A感覚
ELSEIF X == 3
	CALL DECIDE_ABLUP3
;従順
ELSEIF X == 10
	CALL DECIDE_ABLUP10
;欲望
ELSEIF X == 11
	CALL DECIDE_ABLUP11
;技巧
ELSEIF X == 12
	CALL DECIDE_ABLUP12
;奉仕技術
ELSEIF X == 13
	CALL DECIDE_ABLUP13
;性交技術
ELSEIF X == 14
	CALL DECIDE_ABLUP14
;話術
ELSEIF X == 15
	CALL DECIDE_ABLUP15
;奉仕精神
ELSEIF X == 16
	CALL DECIDE_ABLUP16
;露出癖
ELSEIF X == 17
	CALL DECIDE_ABLUP17
;サドっ気
ELSEIF X == 20
	CALL DECIDE_ABLUP20
;マゾっ気
ELSEIF X == 21
	CALL DECIDE_ABLUP21
;レズっ気
ELSEIF X == 22
	CALL DECIDE_ABLUP22
;ＢＬっ気
ELSEIF X == 23
	CALL DECIDE_ABLUP23
;性交中毒
ELSEIF X == 30
	CALL DECIDE_ABLUP30
;自慰中毒
ELSEIF X == 31
	CALL DECIDE_ABLUP31
;精液中毒
ELSEIF X == 32
	CALL DECIDE_ABLUP32
;レズ中毒
ELSEIF X == 33
	CALL DECIDE_ABLUP33
;売春中毒
ELSEIF X == 37
	CALL DECIDE_ABLUP37
;獣姦中毒
ELSEIF X == 39
	CALL DECIDE_ABLUP39
ENDIF
IF RESULT == 1
	PRINT  *
ELSEIF ABL:X >= 10
	PRINT  
ELSE
	PRINT  
ENDIF

RETURN 1

@USERABLUP
IF RESULT == 999
	CALL JUJUN_UP_CHECK
	CALL YOKUBO_UP_CHECK
	BEGIN TURNEND
	RETURN 1
ENDIF

RETURN 0
;
