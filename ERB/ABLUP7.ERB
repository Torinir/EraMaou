
@ABLUP7
DRAWLINE
IF ABL:7 >= 5
	PRINTW Already At MAX..
	RETURN 0
ENDIF

;条件別にＯＫかダメかを記録する
I = 0
J = 0
K = 0

IF ABL:7 == 0
	A = 100
ELSEIF ABL:7 == 1
	A = 1000
ELSEIF ABL:7 == 2
	A = 5000
ELSEIF ABL:7 == 3
	A = 15000
	;一線越えない
	SIF TALENT:27
		TIMES A , 2.00
ELSEIF ABL:7 == 4
	A = 35000
	;一線越えない
	SIF TALENT:27
		TIMES A , 3.00
ENDIF

;倒錯的
IF TALENT:80
	TIMES A , 0.75
ENDIF

;目立ちたがり
IF TALENT:80
	TIMES A , 0.50
ENDIF

;欲望が露出癖+1レベルでないといけない
PRINTS ABLNAME:1
PRINTV ABL:7+1
PRINTL LV以上,かつ
IF ABL:1 < ABL:7+1
	;欲望が不足
	I |= 4
ENDIF

;ＬV３から４,４から5に上げるときは異常経験必要(素質:[目立ちたがり]なら無視できる)
IF ABL:7 == 3 && TALENT:28 == 0
	PRINTS EXPNAME:50
	PRINTL あり,かつ
	IF EXP:50 == 0
		;異常経験が不足
		I |= 2
	ENDIF
ELSEIF ABL:7 == 4 && TALENT:28 == 0
	PRINTS EXPNAME:50
	PRINTL 2以上,かつ
	IF EXP:50 < 2
		;異常経験が不足
		I |= 2
	ENDIF
ENDIF

;恥情の珠で上げる
SIF JUEL:8 < A
	I |= 1
IF ABL:7 < 2
	;絶頂経験が必要
	SIF EXP:2 == 0
		I |= 2
ELSE
	;調教自慰経験が必要
	SIF EXP:11 == 0
		I |= 2
ENDIF

PRINT [0] - 
PRINTS PALAMNAME:8
PRINT の珠×
PRINTV A
IF ABL:7 < 2
	PRINT ,
	PRINTS EXPNAME:2
	PRINTV 1
	PRINT 以上
ELSE
	PRINT ,
	PRINTS EXPNAME:11
	PRINTV 1
	PRINT 以上
ENDIF
PRINT ......

IF I == 0
	PRINT OK
ELSE
	SIF I & 1
		PRINT Not enough points 
	SIF I & 2
		PRINT Not enough exp 
	SIF I & 4
		PRINT 能力不足 
ENDIF
PRINTL 


PRINTL [100] - やめる


INPUT
IF (RESULT < 0 || RESULT > 0) && RESULT != 100
	RESTART
ELSEIF I != 0 && RESULT == 0
	PRINTL Requirements haven't been met.
	RESTART
ELSEIF RESULT == 100
	RETURN 0
ENDIF

ABL:7 += 1

IF RESULT == 0
	JUEL:8 -= A
ENDIF

PRINTS ABLNAME:7
PRINT のレベルが
PRINTV ABL:7
PRINTW になりました.

