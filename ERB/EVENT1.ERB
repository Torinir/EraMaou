@EVENTCOMEND

SIF CFLAG:TARGET:100
	RETURN 0

IF EXP:0 == 0 && EXP:1 > 0 && TALENT:30
	CFLAG:TARGET:100 = 1
	PRINTW (Looks like they are content with having their ass ravaged a bit as long as they get to protect their virginity---)
ENDIF
